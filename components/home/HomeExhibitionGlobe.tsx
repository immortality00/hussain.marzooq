// Homepage section 2 — the exhibition globe, directly under the hero.
// D2b creates the mount point only; D6 fills it (react-globe.gl + a ranked city
// index built on getExhibitionCities). Until then, empty means empty: this
// renders nothing at all, not a placeholder and not a panel. See CLAUDE.md →
// "Globe" and SESSION-QUEUE.md §D6.
export function HomeExhibitionGlobe() {
  return null;
}
