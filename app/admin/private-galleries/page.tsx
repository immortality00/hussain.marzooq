export default function AdminPrivateGalleriesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Private Galleries</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Coming next: create a gallery with a secret link + password, select media,
        expiry optional, and download requests always require your approval.
      </p>

      <div className="mt-8 rounded-2xl border p-6 text-sm text-muted-foreground">
        Placeholder: Private galleries dashboard.
      </div>
    </div>
  );
}
