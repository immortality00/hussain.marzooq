export default function AdminServicesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Services</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Coming next: manage service pages, optional “starting from” price (hidden
        if empty), FAQs, and show related work + testimonials.
      </p>

      <div className="mt-8 rounded-2xl border p-6 text-sm text-muted-foreground">
        Placeholder: Services dashboard.
      </div>
    </div>
  );
}
